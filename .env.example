CREDENTIAL_ENCRYPTION_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
PRODUCT_DOMAIN=
# AI keys are added securely inside Folio > AI providers and encrypted per account.
# Keep all secrets server-side. Never add NEXT_PUBLIC_ prefixes to secret keys.
